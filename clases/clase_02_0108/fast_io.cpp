#include <bits/stdc++.h>
#define fast_io ios :: sync_with_stdio(0); cin.tie(NULL);
using namespace std;

int main() {
    //ios :: sync_with_stdio(0); // no utilizaremos la libreia stdio
    //cin.tie(NULL); //desactivar limpieza de buffer por cin
    fast_io
    int x;
    for (int i = 0; i < 3; ++i) {
        cin >> x;
        cout << x << "\n";
    }
    return 0;
}